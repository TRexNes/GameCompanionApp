<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.rex.gameapp.MainActivity"
    android:background="@drawable/button">

    <Button
        android:text="Player Manager"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:id="@+id/manager_button"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:onClick="openPlayer"
        android:background="@drawable/button"/>

    <Button
        android:text="Roll Dice"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:id="@+id/coin_button"
        android:layout_below="@+id/manager_button"
        android:layout_alignParentStart="true"
        android:onClick="openRollDice"
        android:background="@drawable/button"/>

    <Button
        android:text="Timer"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:id="@+id/timer_button"
        android:layout_below="@+id/coin_button"
        android:layout_alignParentStart="true"
        android:onClick="openTimer"
        android:background="@drawable/button"/>

    <ImageView
        android:id="@+id/background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:src="@drawable/gamepiece"
        android:layout_below="@+id/timer_button"
        android:layout_alignParentStart="true">
    </ImageView>



</RelativeLayout>
